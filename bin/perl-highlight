#!/usr/bin/env perl
use perl5i::2;
package perl::highlight;
# ABSTRACT: command-line syntax highlighter for Perl source code

use App::perl::highlight;
use Getopt::Long;
use Pod::Usage;

my %opts = ();
GetOptions( \%opts,
    'help|?',
    'version',
    'html',
);

pod2usage(
    -verbose => 2,
) if $opts{'help'};

=head1 SYNOPSIS

    perl-highlight < file.pl
    sprunge ID
    sprunge --help

=head1 DESCRIPTION

B<perl-highlight> is a simple command line program to do
syntax highlighting on your Perl source code. L<Syntax::Highlight::Perl::Improved>
and L<Term::ANSIColor> do the heavy lifting.

=head2 Running it

Provide input on stdin to B<perl-highlight> to highlight it to stdout with ANSI colour
escapes. Provide the B<--html> option to output HTML (fragment).

    perl-highlight --html < script.pl
    perl-highlight < lib/My/Module.pm

=head1 OPTIONS

=over 4

=item B<--help>, -h, -?

Opens this man page and exits.

=item B<--version>

Prints the version of this program and supporting libraries.

=item B<--html>

Output HMTL fragment instead of ANSI terminal escapes.

=back

=cut

App::perl::highlight->new()->run(\%opts, \@ARGV);

